<UserControl x:Class="WoT_Win.Init.LoadedGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="110" Width="500">
    <Grid>
        <Image Source="{Binding Path=Faceset}" Width="96" Height="96" HorizontalAlignment="Left"/>
        <TextBlock Text="{Binding Path=PlayerName}" Width="200" Height="30" VerticalAlignment="Top" Margin="10"/>
        <TextBlock Text="{Binding Path=AreaName}" Width="200" Height="30" VerticalAlignment="Center" Margin="10"/>
        <TextBlock Text="{Binding Path=SceneName}" Width="200" Height="30" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBlock Text="{Binding Path=Savetime}" Width="200" Height="30" VerticalAlignment="Bottom" Margin="10"/>
    </Grid>
</UserControl>
