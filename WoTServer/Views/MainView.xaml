<Window x:Class="WoTServer.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainView" Height="600" Width="800">
    <Grid>
        <TabControl Margin="20,20,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="500" Height="480">
            <TabItem Header="Clients"></TabItem>
        </TabControl>
        <TextBox Text="{Binding ServerInfo}" Margin="540,20,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Width="240" Height="480" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        <Button Content="Clear" Command="{Binding Path=BtnClearServerInfoCommand}" Width="90" Height="30" VerticalAlignment="Bottom" Margin="620,0,70,20"/>
        <Button Content="Start" Command="{Binding Path=BtnStartCommand}" Width="90" Height="30" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="20,0,0,20"/>
        <Button Content="Stop" Command="{Binding Path=BtnStopCommand}" Width="90" Height="30" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="140,0,0,20"/>
    </Grid>
</Window>
